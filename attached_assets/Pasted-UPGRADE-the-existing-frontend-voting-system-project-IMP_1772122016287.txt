UPGRADE the existing frontend voting system project.

IMPORTANT:
• Do NOT rebuild from scratch
• Extend existing UI and architecture
• Keep backend mocked (Firebase-ready placeholders)
• Preserve existing email verification and photo verification UI
• Implement role-based eligibility and multi-election structure

========================
NEW FEATURES TO ADD
===================

1. MULTI-ELECTION SYSTEM
   Add election entity with fields:
   • electionId
   • electionType (society | college | company)
   • categories[]
   • isActive
   • createdBy
   • startTime
   • endTime

2. ROLE-BASED SIGNUP FLOW
   Upgrade signup page:
   • Add role selector (Student | Employee | Society Member)
   • Add dynamic ID input field:
   Student → Student ID
   Employee → Employee ID
   Society → Voter ID
   • Store role + identifier in session store
   • Add eligibility badge UI

3. USER DASHBOARD
   Create dashboard showing:
   • Active elections filtered by role
   • Election cards
   • Election category selector
   • Voting status per category
   • Locked election badge if not eligible

4. ELECTION CATEGORY FLOW
   Add category selection screen:
   • Categories displayed as cards
   • Category vote status indicator
   • Progress UI

5. CANDIDATE DETAIL PAGE
   Add candidate detail screen with:
   • Candidate profile
   • Advantages
   • Disadvantages
   • Manifesto section
   • Image
   • Vote CTA button

6. SECURITY VERIFICATION STEP
   Before vote submission:
   • Email verification stepper
   • Camera capture modal (reuse existing)
   • Identity verified badge
   • Secure action confirmation modal

7. ADMIN ELECTION CREATION
   Upgrade admin panel:
   • Election creation wizard
   • Election type selector
   • Category creator UI
   • Candidate per category mapping

8. ADMIN ELECTION LIFECYCLE
   Add controls:
   • Start election toggle
   • Stop election toggle
   • Election status badge
   • Countdown timer UI

9. MOCK SECURITY LOGIC
   Simulate:
   • Role eligibility check
   • One vote per category
   • Election active guard
   • Duplicate voting guard
   • Session fingerprint indicator

10. STATE MANAGEMENT UPDATE
    Extend global store with:
    • elections[]
    • categories[]
    • userRole
    • userIdentifier
    • eligibilityFlags
    • categoryVoteStatus

11. ROUTE GUARDS
    Add guards for:
    • Eligibility protection
    • Election active check
    • Category vote lock
    • Verification required before voting

12. UI REQUIREMENTS
    • Step-based voting flow
    • Wizard style election creation
    • Role-based UI badges
    • Election eligibility tags
    • Category vote progress
    • Security banners
    • Smooth transitions

13. FIREBASE READY HOOKS
    Create placeholders:
    • useElectionEligibility()
    • useCategoryVoting()
    • useUserRole()
    • useElectionLifecycle()
    • useCategoryResults()

14. LOCAL STORAGE MOCK
    Persist:
    • role + identifier
    • voted categories
    • election state
    • verification state

========================
OUTPUT
======

Return upgraded working frontend with:
• role-based signup
• multi-election dashboard
• category voting flow
• candidate detail pages
• security verification step
• admin election lifecycle
• route guards
• firebase-ready architecture
